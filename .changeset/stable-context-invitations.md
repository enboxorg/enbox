---
"@enbox/api": patch
"@enbox/browser": patch
---

Derive the managed context-invitation inbox from encrypted role audiences instead of application role-group metadata, so current apps sharing one base protocol install the same wire definition.
