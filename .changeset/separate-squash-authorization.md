---
"@enbox/dwn-sdk-js": patch
---

Keep an explicit `squash` action separate from `create` authorization. A
`create` fallback remains available for protocol paths that define no explicit
`squash` action.
