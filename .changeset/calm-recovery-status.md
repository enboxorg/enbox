---
"@enbox/agent": patch
"@enbox/api": patch
---

Persist replication recovery failures and retry deadlines, expose them through sync status, and include the durable reason when paused application views report an error.
