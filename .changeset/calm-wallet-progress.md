---
"@enbox/agent": patch
---

Expose connect approval phase progress and split protocol, permission grant, and grant-key performance timings so wallet UIs can show where a long approval is spending time.
