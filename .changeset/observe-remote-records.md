---
"@enbox/api": patch
"@enbox/browser": patch
---

Allow typed record views to observe bounded queries on a foreign DWN tenant with `from` and `protocolRole`, reusing the existing remote subscription and canonical query materialization path.
