---
'@enbox/dwn-sdk-js': patch
---

Return a bounded, server-proven dependency closure when an authenticated role holder reads a context root for replica bootstrap. Include ancestry-only role proofs for role-signed writes and deletes so one member's mutations can be admitted by every other member replica.
