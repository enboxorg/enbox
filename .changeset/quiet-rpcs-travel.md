---
'@enbox/dwn-clients': patch
---

Add an injectable, all-or-nothing HTTP and WebSocket network transport for built-in RPC clients, with isolated socket lifecycle management. Registration helper networking is unchanged.
