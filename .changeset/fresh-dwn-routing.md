---
"@enbox/dids": patch
"@enbox/agent": patch
"@enbox/auth": patch
"@enbox/api": patch
"@enbox/browser": patch
---

Resolve and cache authoritative DID-advertised DWN endpoints across identity recovery, routing, sync, connect, and browser DRL flows. Expose typed endpoint-readiness errors and live endpoint-change refresh signals while preserving DID documents unless an endpoint replacement is explicitly requested.
