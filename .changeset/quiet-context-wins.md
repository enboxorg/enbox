---
'@enbox/agent': patch
'@enbox/dwn-server': patch
---

Classify data replays against the receiver's committed winner before quota, preserve root-versus-dependency push-failure context, and log reconciliation failures once at their workflow owner.
