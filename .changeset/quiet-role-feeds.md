---
'@enbox/dwn-sdk-js': patch
---

Authorize exact-context, multi-path message feeds through an active protocol role, including delegated authors, delivery-time membership checks, and delete ancestry needed by replicas. Internal membership-only wake events remain hidden; an event that also matches a caller-requested filter is delivered once, and scoped subscribers do not receive tenant-wide feed-head metadata.
