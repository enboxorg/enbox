---
"@enbox/dwn-sdk-js": patch
---

fix: resolve retained initial writes by stable entry ID, omit incomplete query and subscription snapshot records without shortening cursor pages, and apply the same logical-record projection to counts
