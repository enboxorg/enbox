{
  "name": "@enbox/electrobun-dwn",
  "version": "0.0.6",
  "private": true,
  "type": "module",
  "description": "Electrobun app that runs a local Enbox DWN server",
  "scripts": {
    "prebuild:dwn": "bun run --filter @enbox/common build && bun run --filter @enbox/crypto build && bun run --filter @enbox/dids build && bun run --filter @enbox/dwn-sdk-js build && bun run --filter @enbox/dwn-clients build && bun run --filter @enbox/agent build && bun run --filter @enbox/dwn-sql-store build && bun run --filter @enbox/dwn-server build",
    "dev": "bun run prebuild:dwn && electrobun dev",
    "build": "electrobun build",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0"
  },
  "dependencies": {
    "@enbox/agent": "workspace:*",
    "@enbox/dwn-server": "workspace:*",
    "electrobun": "1.15.1",
    "reflect-metadata": "0.2.2"
  },
  "devDependencies": {
    "@types/bun": "1.3.10"
  }
}
