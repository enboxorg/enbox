{
  "name": "enbox",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/common",
    "packages/crypto",
    "packages/dids",
    "packages/agent",
    "packages/user-agent",
    "packages/proxy-agent",
    "packages/api",
    "packages/identity-agent",
    "packages/browser",
    "packages/dwn-sdk-js",
    "packages/dwn-sql-store",
    "packages/dwn-server"
  ],
  "scripts": {
    "clean": "pnpm clean:modules && pnpm clean:data && pnpm --recursive run clean",
    "clean:modules": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "clean:data": "rm -rf data/ data-test/ __TESTDATA__/ TEST-DATASTORE TEST-MESSAGESTORE TEST-EVENTLOG TEST-RESUMABLE-TASK-STORE TEST-INDEX BENCHMARK-INDEX BENCHMARK-BLOCK DATASTORE MESSAGESTORE EVENTLOG RESOLVERCACHE RESUMABLE-TASK-STORE INDEX DATA/ && find . -name '*.sqlite' -o -name '*.db' -o -name 'dwn.db' | xargs rm -f",
    "postinstall": "node scripts/rebuild-native-if-needed.cjs",
    "rebuild:native": "pnpm config set build.onlyBuiltDependencies '[]' && cd packages/dwn-server && pnpm rebuild better-sqlite3 && cd ../dwn-sql-store && pnpm rebuild better-sqlite3 && cd ../..",
    "prebuild": "pnpm rebuild:native",
    "build": "pnpm --recursive --stream build",
    "build:ci": "./scripts/ci-setup.sh",
    "test:node": "pnpm --recursive test:node",
    "lint": "pnpm --recursive lint",
    "lint:fix": "pnpm --recursive lint:fix",
    "setup": "git submodule update --init --recursive && pnpm install",
    "setup:examples": "git submodule update --init --recursive && cd examples/web-wallet && pnpm install --ignore-workspace && cd ../dapp-demo && pnpm install --ignore-workspace",
    "dev:web-wallet": "cd examples/web-wallet && ./start-dev.sh",
    "dev:dapp-demo": "cd examples/dapp-demo && pnpm run dev"
  },
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=8.0.0"
  },
  "volta": {
    "node": "20.10.0",
    "pnpm": "8.15.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/enboxorg/enbox.git"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "npkill": "^0.12.2"
  }
}